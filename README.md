# AI-Assisted-Support-Ticket
Used the Claude API and Pandas to automatically categorize customer support requests into groups like billing, login issues, data, and integrations. Built a testing step comparing AI tags against human-labeled tickets to make sure the model was reliable before use.
